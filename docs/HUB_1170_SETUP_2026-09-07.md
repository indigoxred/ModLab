# 1.6.1170 setup delivery — 7 September 2026

The machine now runs the Steam 1.6.1170 executable with its matched launcher and shaders, using wSkeever's existing Best of Both Worlds patcher through ModLab's new setup launcher. The official game data and free Creation content remain installed. This is not a complete old-data reinstall.

## Delivered and observed

- Setup / tool locations → Skyrim 1.6.1170 setup / downgrade: upstream download links, verified source-file backups, a hash-pinned external patcher launcher with the correct working directory, and read-only baseline checks. It refuses wrong source files/store, changed patcher executables and an existing patcher backup directory. Repeated launch is disabled after starting.
- Read the actual patcher source and checksum file. The executable, launcher and shader archive all match its expected 1.6.1170 SHA-256 values after patching. The game executable also matches Reliquary's independent Steam SHA-1 identity. No stale ContentCatalog.txt was present.
- Corrected a real upstream link defect: the patcher's SKSE 2.2.8 link points to file 792372, which downloads 2.3.0. ModLab uses file 792256. Its archive contains skse64_2_02_08 and skse64_1_6_1170.dll; its changelog identifies 2.2.8. ModLab's existing recoverable installer now recognizes this exact official archive.
- USSEP 4.3.8a downloaded from official archived Nexus file 733846. Its included readme identifies 4.3.8a and supports game version 1.6.1130 and up. It remains a private local download; no USSEP payload is bundled with ModLab.
- ModLab queue `5b259e7a1ea6` installed both SKSE and USSEP into the fresh `ModLab 1170 Baseline` profile. SKSE operation `8820c5b10ece` recorded root-file recovery and 62 effective scripts. USSEP installation record: `app/plugins/data/modlab/installations/34aebb36f948448e826f4f72dd94eb8e.json` in the test instance.
- Automatic setup `e30d0699134e` ran LOOT and five xEdit checks. Existing narrow cleaning behavior created private Fishing and Survival copies and verified them; original game plugins were not cleaned in place. Saints & Seducers retains its previously observed record warning. USSEP's record check reported zero errors. Remaining Review findings mean the queue correctly retains Needs attention rather than declaring every behavior verified.
- Launch `61216c126e50` went through ModLab/MO2's SKSE loader. The recorded startup verified SKSE 2.2.8 initialized on 1.6.1170 with no reported native-load failures (this minimal profile selected no additional native DLLs). Skyrim rendered its main menu at 1920×1080. No save was loaded or created, and no gameplay feature was verified.
- The native desktop tool still failed to deliver game keyboard input reliably. After the menu check, that specific disposable Skyrim process was stopped. The upstream patcher likewise reached its informational Backup Complete dialog; all three file hashes and catalog state were verified before its inaccessible completion-dialog processes were stopped. These are limitations of this unattended UI test, not evidence of a game compatibility failure.

## Preserved state

The old `ModLab Test` profile and its `ModLab 1170 Test` copy remain. Its mod activation list, load order and game INIs match the pre-migration copy; MO2 rewrote only comments in plugins.txt, with the plugin entries unchanged. Old mods and generated output remain installed but are not silently treated as compatible with the downgraded game. `ModLab 1170 Baseline` is the current selected profile.

ModLab's game backups are under `workspace/hub-test-20260906/app/plugins/data/modlab/game-backups/08b4c05444ae` and `7289e592a8f7`. Each has backup.json and verified original game/root-helper files. The patcher also retained `1.7.104 backup` and `1.6.1170 backup` inside the game directory. The SKSE installer retains the previous loader separately in its operation directory. The prior source/tests/docs snapshot is `outputs/pre-1170-source-20260907.zip` in the Codex task directory, SHA-256 `fccc415180acacdc82209d28924a651b1a7345ded80ad4c7f9fd48048b63513b`.

Package identities:

| Download | SHA-256 |
|---|---|
| Upstream patcher archive 1.7.104.3 | 047f470a51b36964a24f11129ca7a44f4e982a0c20a576e044b62ac29fd2f41c |
| Steam SKSE 2.2.8 | ada6a771e18245cd0a6106b7e019b3b2bdf7a6bee390bc069c6e0a35f89544fa |
| USSEP 4.3.8a | 5036e8b4abef7234a1f35d43e33780f529be8f85f449e41211ec4876d03e95d9 |

The downloaded Address Library v13 archive already contains versionlib-1-6-1170-0.bin despite its 1.7.104 filename. Do not replace or reject it based solely on its title.

## Next work, not completed by this setup delivery

1. Rematch the copied full profile's actual native components and version-bound dependencies to 1.6.1170. Do not enable its old 1.7-only DLLs or reuse an existing save as proof of compatibility.
2. Continue testing ModLab itself on the deliberately clashing Desktop/Mods set. Improve actionable advice, including the excessive generic Review rows for USSEP's expected replacements. This test folder is not the user's eventual intended build.
3. Preserve the existing queue/FOMOD, LOOT, narrow cleaning, BodySlide, Pandora, SKSE, Synthesis, PGPatcher, archive/record inspection, NPC choices and recovery work. Unfinished Wrye Bash, TexGen/DynDOLOD, helper acquisition, broader animation choices and queued dependency/output invalidation work remains unfinished.
4. Commit/publish the local alpha work separately. GitHub still does not contain this delivered local alpha; the pre-existing uncommitted work was preserved rather than swept into a bulk commit.

The one-hour allowance covered this setup route and focused verification; it finished in approximately 55 minutes. Fifteen focused setup/SKSE/launch tests passed. All 55 source modules match the deployed plugin. Package `dist/ModLab-Hub-0.1.0-alpha-1170-20260907-0344.zip` passed all 57 members' readback checks; SHA-256 `8913391eab7dc97dff145befa1d9549bd00d1a8a993050a5f54f3943f6a3f787`. Game and MO2 are closed. This is not an unlimited extension for the remaining product work or evidence that the full prototype is complete.
